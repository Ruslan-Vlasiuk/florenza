/**
 * Slow-drifting blurred-blob mesh behind the entire site.
 * Two huge radial gradients (rose + sage) drift atomically slowly,
 * giving the cream page subtle atmospheric depth without being noticed.
 */
export function AuroraBackground() {
  return <div className="aurora-bg" aria-hidden="true" />;
}
